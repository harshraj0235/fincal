# DISABLED: GitHub Actions workflow disabled due to billing limitations
# Use manual-blog-generator.cjs instead for blog generation
# Use local build and deployment scripts

name: DISABLED - Build and Test

on:
  # Disabled all triggers to prevent billing issues
  workflow_dispatch: # Only manual trigger allowed

jobs:
  # This job is disabled
  disabled:
    runs-on: ubuntu-latest
    name: DISABLED - Build and Test
    
    steps:
      - name: Workflow Disabled
        run: |
          echo "❌ This workflow is disabled due to GitHub Actions billing limitations"
          echo "✅ Use manual-blog-generator.cjs for blog generation instead"
          echo "📖 See BLOG_GENERATION_README.md for setup instructions"
          exit 1
